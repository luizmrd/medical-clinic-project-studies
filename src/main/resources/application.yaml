spring:
  application:
    name: medical-clinic-project
  datasource:
      url: jdbc:h2:mem:testdb
      username: admin
      password:
  h2:
    console:
        enabled: true
        path: /h2-console
  jpa:
      open-in-view: true
      show-sql: true
      properties:
        hibernate.format_sql: true